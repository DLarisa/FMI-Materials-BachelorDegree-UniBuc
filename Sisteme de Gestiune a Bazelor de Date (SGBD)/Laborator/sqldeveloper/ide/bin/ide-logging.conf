#
# Nondebug logging configuration for JDeveloper.
#
# In nondebug builds, we mostly use the standard logging configuration.
# INFO level messages are logged to the IDE's log window, however.
#
handlers=java.util.logging.ConsoleHandler
.level=WARNING

